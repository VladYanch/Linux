Linux 15.09

1) полной очистки индекса git reset HEAD

2) для просмотра разницы между раб.каталогом и посл. коммитом git diff
	


3) для создания новой ветки и одновременного переключения в на нее 
	git checkout -b vetka

4) для сравнения веток между собой git diff vetka1 vetka2

5) для удаления локальной ветки git -d branch vetka

6) для слияния веток git merge test

7) для перезаписи посл/коммита git commit --amend 'new'


Коротко опишите причину возникновения конфликтов при слиянии и порядок действий для их решения.

В каких случаях происходит MERGE CONFLICT? В случае, когда с одном из двух git-ов не было окончательного коммита или файл был изменен в обоих ветках
Tогда git merge master vetka,  после ручная регулировка и после еще раз коммит.